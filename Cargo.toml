[package]
name = "thesilentreach"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = "0.15"
big_space = "0.8"
rand = "0.8"
tokio = { version = "1.0", features = ["rt-multi-thread", "sync", "macros"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-trait = "0.1"

